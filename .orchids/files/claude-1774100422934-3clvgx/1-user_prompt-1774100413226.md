Make the following changes to the site. Do not touch the Jesus Kids card or the Jesus Kids page at all.

1. YELLOW CARDS — Change ALL yellow cards across the entire site to the same blue as the "Watch More" card on the Msg page (#64B5F6). This includes the Invite card on the Home page and any other yellow cards on any page. Match the exact same blue color and style as the Watch More card.

2. SECONDARY TEXT COLOR — On ALL cards except the Jesus Kids card, change the secondary text (labels like "Welcome", "Share", "Watch", subtitles like "Bring a friend along", "This week's sermon", "We'd love to connect with you", "All messages", "Via WhatsApp") to dark grey (#333333). The bold titles stay white. Only the Jesus Kids card is excluded from this change.

3. STAGGERED CARD ENTRANCE — On every page, cards should not appear all at once. Each card should fade in and rise up from below (translateY 20px to 0, opacity 0 to 1) one after another with a 0.1s delay between each card. The animation should be smooth (0.4s ease-out). This should happen every time a page is navigated to, not just on first load.

4. BACKGROUND GRAIN TEXTURE — Add a very subtle noise/grain texture overlay on the dark background (#131212) across the entire site. It should be almost invisible — just enough to give the background a premium matte paper feel instead of flat digital black. Use a CSS noise pattern or a tiny repeating noise image. Very low opacity (3-5%).

5. SKELETON SCREEN — When the site first loads, show pulsing grey placeholder shapes in the exact layout of the cards before the real content appears. The placeholders should pulse with a subtle animated shimmer (dark grey to slightly lighter grey sweep). Once content is ready, crossfade smoothly from skeleton to real cards using the staggered entrance from point 3.

6. OFFLINE MODE — Add a service worker that caches the entire site for offline use. If the user opens the site with no internet connection, they should see the last loaded version of the site. Show a small subtle toast message at the top that says "You're offline — showing saved version" that auto-dismisses after 3 seconds. When connection returns, refresh silently.

7. SMART NAV HIDING — When the user scrolls down on any page, the bottom navigation bar should smoothly slide down and hide off screen. When the user scrolls back up, the bottom nav should smoothly slide back into view. The transition should be fast and smooth (0.3s ease). The nav should always be visible when the user is at the top of the page.

Do not touch the Jesus Kids card or the Jesus Kids page. Do not change any existing layout, navigation items, or functionality beyond what is listed above.